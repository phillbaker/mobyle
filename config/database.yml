defaults: &defaults
  adapter: sqlite

development:
  database: db/development.db
  <<: *defaults

  # Add more repositories
  # repositories:
  #   repo1:
  #     adapter:  postgres
  #     database: sample_development
  #     username: the_user
  #     password: secrets
  #     host:     localhost
  #   repo2:
  #     ...

test:
  database: db/test.db
  <<: *defaults
production:
  # TODO => db/production not #{name}_production
  database: db/ihub_production.db
  <<: *defaults
