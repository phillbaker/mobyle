<%= simple_form_for([@user, @hub, @contact], :html => { :class => 'form-horizontal' }) do |f| %>
	<%= f.input :name %>
	<%= f.input :telephone %>
	
	<div class="form-actions">
		<%= f.button :submit, :class => 'btn-primary' %>
	</div>
<% end %>
