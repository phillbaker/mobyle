<%= simple_form_for([@user, @hub], :html => { :class => 'form-horizontal' }) do |f| %>
	<%= f.input :name %>
	
	<div class="form-actions">
		<%= f.button :submit, :class => 'btn-primary' %>
	</div>
<% end %>
